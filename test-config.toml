[server]
bind_address = "127.0.0.1"
port = 5355
tcp_timeout = 30

[cache]
ttl_seconds = 300
enabled = true

[logging]
level = "debug"

[mdns]
query_timeout_ms = 1500
discovery_timeout_ms = 2500
service_types = [
    "_http._tcp.local.",
    "_ssh._tcp.local.",
    "_device-info._tcp.local.",
]
